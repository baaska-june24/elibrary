query CONTENT_SEARCH(
  $where: SearchWhere!
  $limit: Int
  $offset: Int
  $order_by: [OrderBy]
) {
  search(where: $where, limit: $limit, offset: $offset, order_by: $order_by) {
    id
    name
    average_rate
    cover
    type
    published_at
    price
    payment_type
    created_at
  }
}
